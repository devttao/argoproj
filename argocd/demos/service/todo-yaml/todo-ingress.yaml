apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress-todo
spec:
  rules:
    - host: todo-yaml.myapp.local
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: todo-gitops
                port:
                  number: 8080
